[project]
name = "ai-honeypot-collection"
version = "0.1.0"
description = "Safe, observable decoys for researching automated and AI-assisted security scanning."
requires-python = ">=3.11"
license = { text = "MIT" }

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.pytest.ini_options]
testpaths = ["tests"]
